import React from 'react'

const TImer = () => {
  return (
    <div>
        This will be a timer import React from 'react'
        
        export const Component = (props) 
            return(
                <div>
                    {props.children}
                </div>
            )
        
        
        export default Component
    </div>
  )
}

export default TImer
